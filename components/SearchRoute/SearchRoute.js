import React from 'react';
import { View, TextInput, Text, Image, FlatList, TouchableOpacity, Keyboard } from 'react-native';
import { useState } from 'react';
import styles from './styles';

const SearchRoute = ({ routesData = [], onRouteSelect }) => {

    return (      
        <View style={styles.searchContainer}>

        </View>
    );
};

export default SearchRoute;